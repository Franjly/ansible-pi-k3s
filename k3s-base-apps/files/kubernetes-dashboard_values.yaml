ingress:
  enabled: True

  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
    # kubernetes.io/tls-acme: 'true'

  hosts:
    - kubernetes-dashboard.192.168.40.200.nip.io
  tls:
    - secretName: kubernetes-dashboard-tls
      hosts:
        - kubernetes-dashboard.192.168.40.200.nip.io
