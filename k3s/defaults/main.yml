---
k3s_master:
  hostname: pi-kube-master-0
  ip: 192.168.30.10
  flags:
  - --no-deploy servicelb
  - --no-deploy traefik
  - --node-taint node-role.kubernetes.io/master=true:NoSchedule
  - --node-label kubernetes.io/role=master

k3s_worker:
  flags:
  - --node-label kubernetes.io/role=worker

k3s_version: ''
