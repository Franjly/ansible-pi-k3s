# ansible-pi-change-default-user
Ansible playbook to change the default pi user

## Requirements
1. Python 3
1. Ansible

## Usage
```bash
$ ansible-playbook -i inventory_change_default_user.yml 0_change-default-user.yml
```